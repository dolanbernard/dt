[package]
name = "dt"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45"
chrono-tz = {version = "0.10.4", features = ['case-insensitive']}
clap = {version = "4.6.1", features = ["derive"]}
